import Link from 'next/link'

import { createReservationAction } from '@/features/reservations/actions'
import { getReservationCustomers } from '@/features/reservations/admin-queries'
import { requireAdmin } from '@/lib/auth-session'

export const dynamic = 'force-dynamic'

export default async function NewReservationPage() {
  await requireAdmin('/admin/reservas/nueva')
  const customers = await getReservationCustomers()

  return <main className="admin-shell"><section className="admin-content"><div className="admin-heading"><div><p className="eyebrow">Reservas</p><h1>Nueva reserva</h1><p className="muted">El cliente verá esta información en su cuenta.</p></div><Link className="button" href="/admin/reservas">Volver</Link></div>{customers.length === 0 ? <div className="empty-state"><p>Primero debe existir una cuenta de cliente para asignarle una reserva.</p></div> : <form action={createReservationAction} className="space-y-6 rounded-3xl border border-stone-200 bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold text-stone-800">Cliente<select required name="userId" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal"><option value="">Selecciona un cliente</option>{customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name || customer.email} — {customer.email}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Tipo<select required name="kind" defaultValue="hotel" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal"><option value="hotel">Hotel</option><option value="tour">Tour</option><option value="excursion_national">Excursión nacional</option><option value="excursion_international">Excursión internacional</option><option value="cruise">Crucero</option></select></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Título<input required name="title" maxLength={160} placeholder="Ej. Hyatt Ziva Cap Cana" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Destino<input required name="destination" maxLength={120} placeholder="Punta Cana, República Dominicana" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Fecha de inicio<input name="startsOn" type="date" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Fecha final<input name="endsOn" type="date" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Viajeros<input required name="travelerCount" type="number" min="1" max="30" defaultValue="1" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Estado<select required name="status" defaultValue="pending" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal"><option value="pending">Pendiente</option><option value="confirmed">Confirmada</option><option value="completed">Completada</option><option value="cancelled">Cancelada</option></select></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Proveedor<input name="provider" maxLength={80} placeholder="Grupo González" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Referencia del proveedor<input name="providerReference" maxLength={120} className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Total acordado<input name="totalAmount" type="number" min="0" step="0.01" className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><label className="grid gap-2 text-sm font-semibold text-stone-800">Moneda<input name="currency" defaultValue="USD" minLength={3} maxLength={3} className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal uppercase" /></label></div><label className="grid gap-2 text-sm font-semibold text-stone-800">Nota visible para el cliente<textarea name="customerNote" maxLength={1000} rows={4} placeholder="Incluye solo información que el cliente debe ver." className="rounded-xl border border-stone-200 px-3 py-2.5 font-normal" /></label><div className="flex justify-end gap-3 border-t border-stone-100 pt-5"><Link className="button" href="/admin/reservas">Cancelar</Link><button className="button button-sun" type="submit">Guardar reserva</button></div></form>}</section></main>
}
